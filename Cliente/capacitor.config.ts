import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.biblioTec.app',
  appName: 'BiblioTec',
  webDir: 'dist/sgca/browser'
};

export default config;
